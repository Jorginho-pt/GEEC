
// Criar a variável de alvo
target_ = Player; // Certifique-se de que 'Player' é o nome correto da instância do jogador
width_ = camera_get_view_width(view_camera[0]);
height_ = camera_get_view_height(view_camera[0]);

